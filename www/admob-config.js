// Power Snip! — AdMob configuration (NATIVE BUILD ONLY)
// The game reads this before wiring window.PowerSnipAds.
// Until you paste your REAL ad unit IDs below, Google TEST ads are served.
// Real IDs: AdMob console -> Apps -> Power Snip! -> Ad units.
window.POWERSNIP_ADMOB = {
  // rewardedId:     'ca-app-pub-XXXXXXXXXXXXXXXX/XXXXXXXXXX',
  // interstitialId: 'ca-app-pub-XXXXXXXXXXXXXXXX/XXXXXXXXXX',
  testMode: true   // <-- set to false ONLY when the real IDs above are filled in
};
